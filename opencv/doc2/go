(
  pdflatex '\newcommand{\genc}{true}\newcommand{\genpy}{false}\input{opencv.tex}' &&
  bibtex opencv &&
  makeindex opencv &&
  pdflatex '\newcommand{\genc}{true}\newcommand{\genpy}{false}\input{opencv.tex}' > 1
  ) || exit 1
gv --page=144 opencv.pdf &

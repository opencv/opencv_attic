make && grep klak CvReference-py.tex
exit
make &&
(
  pdflatex '\newcommand{\genc}{true}\newcommand{\genpy}{true}\input{opencv.tex}' &&
  bibtex opencv &&
  makeindex opencv &&
  pdflatex '\newcommand{\genc}{true}\newcommand{\genpy}{true}\input{opencv.tex}' > 1
  ) || exit 1

